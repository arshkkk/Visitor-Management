<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.visitormanagement">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Meeting_Booked">

        </activity>
        <activity android:name=".Booking_Meeting">

        </activity>
        <activity android:name=".Verify_OTP" />
        <activity android:name=".Phone_Auth" />
        <activity android:name=".Camera" />
        <activity android:name=".Setting_Up_Meeting">

        </activity>
        <activity android:name=".ShowDetails" />
        <activity android:name=".GetVisitorDetails" />
        <activity android:name=".PhoneAuth" />
        <activity android:name=".Welcome_Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>